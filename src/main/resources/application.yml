spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/online_shopping
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    properties:
      hibernate:
        default_schema: electronics_store
        format_sql: true
        show_sql: true
        hbm2ddl:
          auto: create
